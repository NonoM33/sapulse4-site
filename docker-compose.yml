services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bkpulse
      POSTGRES_PASSWORD: bkpulse_dev
      POSTGRES_DB: bkpulse
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
